server:
  port: 9002

dubbo:
  application:
    name: user   #注册中心name
  protocol:
    name: dubbo  #协议
    port: -1    #系统自动选择的端口

  registry:      # 注册中心
    address: nacos://127.0.0.1:8848    # 注册中心地址
    username: nacos
    password: nacos

